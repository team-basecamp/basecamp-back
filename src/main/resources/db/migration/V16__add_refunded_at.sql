-- =============================================================
--  캠핑 플랫폼 DB 마이그레이션 V16
--  Base    : V1~V16 적용 완료 상태 기준
--  DB      : MySQL 8.0+
--  변경     : 1) payments refunded_at 추가
--  참고     : Flyway 는 접속 URL 의 DB 로 자동 실행됨.
--            Workbench 로 직접 테스트할 때는 먼저 camping_db 를 선택할 것.
-- =============================================================

-- -------------------------------------------------------------
-- 1. payments 테이블 환불 완료 일시(refunded_at) 컬럼 추가
--    - 스케줄러를 통한 자동 반려 및 환불 시 환불 일시 기록 목적
--    - 기존 결제 건과의 호환성을 위해 NULL 허용으로 생성
-- -------------------------------------------------------------
ALTER TABLE payments 
    ADD COLUMN refunded_at DATETIME NULL COMMENT '환불 완료 일시' AFTER status;